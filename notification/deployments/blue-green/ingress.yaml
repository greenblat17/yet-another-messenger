apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: notification-ingress
  namespace: notification
spec:
  rules:
  - host: messenger.notification
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: blue-service-notification
            port:
              number: 83