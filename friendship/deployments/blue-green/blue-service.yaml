apiVersion: v1
kind: Service
metadata:
  name: blue-service-friendship
  namespace: friendship
spec:
  selector:
    app: friendship
    version: blue
  ports:
  - protocol: TCP
    port: 82
    targetPort: 8082