apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: chat-ingress
  namespace: chat
spec:
  rules:
  - host: messenger.chat
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: blue-service-chat
            port:
              number: 81